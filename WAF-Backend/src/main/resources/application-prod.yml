spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:wafap}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER:wafap_user}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Logging for production
logging:
  level:
    root: WARN
    com.wafap: INFO
    org.springframework.security: WARN
  file:
    name: /var/log/wafap/wafap.log
    max-size: 10MB
    max-history: 30
